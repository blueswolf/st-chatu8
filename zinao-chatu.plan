#主要设置

# 生图提示词设置
## 在这里选择- 随正文一起输出提示词 或 -双击聊天记录获取生图提示词（滑块切换，下面附上两种模式的详细解释）
下方提供两个选项卡，随正文一起输出模式，双击聊天记录模式。滑块选择哪个，哪个选项卡会被点亮，点击选项卡进入详细设置。
### 随正文模式
#### 角色描述（可添加，可设置关键词激活，方便多个角色时，自动选择性插入）
#### 生图提示词
#### 尾部提示词（留空备用）

### 双击聊天记录模式
#### 提示词模型选择（端点-APIkey-获取模型-测试连接）
#### 破限提示词
#### 生图提示词
#### 用户要求提示词（捕获用户在聊天区呼出生图菜单时追加并拼接本栏目输入）
#### 上下文文捕获区（设置楼层，文本如能捕获，展示，但不可编辑）
#### 正文捕获区 （文本如能捕获，展示，但不可编辑）
#### 尾部提示词

#生图模型设置
四个选项 ，单选，点哪个下方四个选项卡哪个被点亮（默认novelAI）
## NOVEL ai
###  固定提示词部分
### NovelAI 设置（去掉氛围转移）
## SD
## ComfyUI
## Banana /Grok
这三个也都去掉提示词替换模块

# 图片缓存改名图片管理，全部保留

# 主题外观 悬浮球图标换一个紫色底大脑 内容保留

# 日志，只保留日志模块  这里才是本插件的运行日志。反馈问题时请导出此处的日志，而不是上方的「错误记录」。写这句话的一块，但是这句话删掉

注意检查代码中有关原插件对文件夹名称的引用(st-chatu8)，要全部更改为本次的新插件（文件夹名称）